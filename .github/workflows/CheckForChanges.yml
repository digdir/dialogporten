name: "Check for changes"

on:
  workflow_call:
    outputs:
      azure:
        description: "Azure related files changed"
        value: ${{ jobs.check-for-changes.outputs.azure }}
      backend:
        description: "Backend related files changed"
        value: ${{ jobs.check-for-changes.outputs.backend }}

jobs:
  check-for-changes:
    name: Filter
    runs-on: ubuntu-latest
    outputs:
      azure: ${{ steps.azure-filter.outputs.src == 'true' }}
      backend: ${{ steps.backend-filter.outputs.src == 'true' }}
    steps:
      - uses: actions/checkout@v4

      - uses: dorny/paths-filter@v2
        id: backend-filter
        with:
          base: ${{ github.ref }}
          filters: |
            src:
              - 'src/**/*'

      - uses: dorny/paths-filter@v2
        id: azure-filter
        with:
          base: ${{ github.ref }}
          filters: |
            src:
              - '.azure/**/*'
            # todo: add additional files that should trigger a build
